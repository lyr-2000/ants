<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ants.dao.UserDao">
    <!-- 插入操作 -->
    <insert id="add" parameterType="User">

--         这个我在ants_base数据库中重新加了一个user表进行测试，因为其他表识别不了
		insert into ants_class_parent (parentId,parentName) values(#{parentId},#{parentName})
	</insert>

</mapper>